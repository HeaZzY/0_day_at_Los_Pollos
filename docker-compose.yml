version: '3'
services:
  myservice:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - DEBUG=1
    # Autres configurations comme les réseaux, les volumes, etc.
